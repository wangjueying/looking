class AppConfig {
  // 环境配置
  static const bool isDebugMode = true;

  // API Key存储键
  static const String apiKeyStorageKey = 'ai_api_key';

  // 默认设置
  static const bool autoSaveResults = true;
  static const bool showConfidenceScore = true;
  static const int defaultItemsPerPage = 20;
}
